# Acceptance Test Report — R3 `library_read`, R4 `hmi_read`, R5 `governance_read` (combined)

Acceptance date: 2026-09-25

Result: **PASS** (read-only) for `main` at the merge of R0–R5 (`d6892c0`, worker unchanged in
`9945418`), the already-open TIA Portal V21 Update 2 project `test.ap21`, and the public MCP calls
recorded here. It also re-checks the R2 port-topology fix.

## Environment

Same host, client, and project as the [R0](2026-09-25-r0-object-read-live.md),
[R1](2026-09-25-r1-plc-read-live.md), and [R2](2026-09-25-r2-hardware-read-live.md) runs
(`--read-only`, MCP stdio). The rebuilt worker required TIA Portal's "Openness access" approval,
given by the operator; the first call of the session (`read_port_topology`) timed out while the
prompt was open, returned `worker_timeout` with its safe-retry message, and succeeded on re-run.

## Automated evidence

- Merged `main`: both builds succeed; 3151 tests pass locally (the 11 known environment-caused
  failures aside); scoped line coverage 92.6% (CI gate 80%); GitHub Actions CI green on `main`.
- `tools/list` in read-only mode: 9 tools — `browse_project_tree`, `execute_read_batch`,
  `get_project_status`, `governance_read`, `hmi_read`, `library_read`, `network_read`,
  `object_read`, `plc_read`.

## Live observations

| Operation | Observed |
| --- | --- |
| `library_read list_libraries` | Project library `test` (root `project`); no global library open |
| `list_library_types` | `logo_groupe_e`, `fpValvePopup`, `fpValve`, `tlProject`, and the script-module folder's type, with `Author`, `Guid`, `Status: Consistent`, `MinimumTargetDeviceVersion` |
| `read_library_type fpValve` | Its versions with `VersionNumber`, `State`, dates, dependencies |
| `check_library_updates` (`includeUpToDate`) | Report for `HMI_RT_1`: `logo_groupe_e 0.0.1` and `fpValve 0.0.6` up to date, with their instances on `sMain` |
| `find_type_instances fpValve` (PLC_1) | 0 instances — `fpValve` is an HMI faceplate; Openness only offers a PLC search scope |
| `list_master_copies` | 0 master copies |
| unknown `libraryName` | `target_not_found`, "libraries are never opened by this tool" |
| `hmi_read list_hmis` | `HMI_RT_1`, runtime `unified`, on `HMI_1` |
| `list_screens` | `sMain` (1024×768, `ScreenNumber 0`) |
| `list_screen_items sMain` | 6 items: `Button_1`, `IO field_1`, `IO field_2`, `Graphic view_2`, `Faceplate container_1`, `Faceplate container_2` with scalar properties |
| `read_screen_scripts sMain` | 1 script: `Button_1` `Tapped` event, JavaScript calling `HMIRuntime.Tags.SysFct.SetBitInTag` |
| `list_hmi_tags`, `list_hmi_connections`, `list_hmi_text_lists`, `list_hmi_scripts` | Tags such as `DbHmi_Valve1_iHmi` with `AccessMode`, `PlcTag`, …; `HMI_Connection`; `Text_list_1`; `Global module` |
| `list_hmi_alarms`, `list_hmi_logs` | Succeeded with 0 items |
| `governance_read list_portal_processes` | PID 3572 `WithUserInterface` on `test.ap21`, `isAttached: true` |
| `read_umac` | Project users (`Anonymous`, `Administrator`, …) with role references (`HMI Monitor`, …), roles with engineering rights |
| `read_safety` | `capability_unavailable` — CPU 1515-2 PN is not fail-safe |
| `list_test_suite`, `list_vci_workspaces` | Succeeded with empty sections |
| `list_multiuser` | Two project servers (local and a network server) with host and port; no local session |
| `network_read read_port_topology` (re-check) | 3 PLC ports, no diagnostics (the R2 non-scalar fix) |

## Not covered live

- Open global libraries, master copies, and PLC-scoped type instances (none in the project).
- WinCC Classic HMIs (none in the project; covered offline).
- A fail-safe CPU, TestSuite content, VCI workspaces, and Multiuser local sessions.
