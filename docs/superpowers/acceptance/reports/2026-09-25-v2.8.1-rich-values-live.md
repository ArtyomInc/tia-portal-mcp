# Acceptance Test Report — v2.8.1 rich attribute values and null supported types

Acceptance date: 2026-09-25

Result: **PASS** (read-only) for branch `fix/v2.8.1-rich-values`, built in Release against the
TIA Portal V21 Update 2 Openness assemblies, driven over MCP stdio (`--read-only`) against the
already-open project `test.ap21`.

## Environment

Same host and project as the [R3–R5 run](2026-09-25-r3-r5-live.md). The client was a scripted
stdio MCP session against `TiaMcpServer.exe` from the Release build; the rebuilt worker required
TIA Portal's "Openness access" approval.

## Automated evidence

- 3197 tests: all pass except the 11 known environment-caused local failures (missing `pwsh`,
  fixture encodings), which pass on GitHub CI.
- New tests: `TiaMcpServer.Tests/ObjectRead/RichValueTests.cs`.

## Live observations

| Call | Before (v2.8.0) | After |
| --- | --- | --- |
| `hmi_read list_screen_items sMain` | 6 items, each `unavailable: ["*"]`, no values | 6 items, `unavailable: []`, 21–34 values each: `BackColor` `{"hex":"#F2F4FF","alpha":255}`, `Text` `{"en-US":"<body><p>Set</p></body>"}`, `ToolTipText`, `ContainedType "V0.0.6\fpValve"`, `Graphic "logo_groupe_e V 0.0.1"` |
| `object_read describe_object Button_1` | 0 attributes | 35 attributes |
| `object_read read_object_attributes Button_1` | `BackColor`/`ForeColor`/`Text` `unrepresentable` | `color` and `multilingualText` kinds |
| `object_read read_object_attributes` on the project | `CreationTime`, `LastModified`, `Comment` `unrepresentable` | `dateTime` (`2026-09-24T11:51:47.7600890Z`) and `multilingualText` kinds |

## Not covered live

- `duration` and `array` kinds (no such attribute was read in the project; covered offline).
